import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
     domains: ['lh3.googleusercontent.com', '127.0.0.1', 'www.jcprola.com', 'img.freepik.com', 'www.softgenix.space'], // agrega aquí todos los dominios externos que uses
  },
};

export default nextConfig;
